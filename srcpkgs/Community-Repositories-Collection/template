# Template file for 'Community-Repositories-Collection'
pkgname=Community-Repositories-Collection
version=1
revision=1
metapackage=yes
short_desc="CRC: Collection of community repositories"
maintainer="zenobit <zen@osowoso.org>"
license="Public Domain"
homepage="https://repo.osowoso.org"

CRC-black-hole_package() {
	archs="x86_64*"
	short_desc="${short_desc} Black Hole repository"
	homepage="https://mirror.black-hole.dev"
	pkg_install() {
		vmkdir etc/xbps.d
		echo "repository=${homepage}/${XBPS_TARGET_MACHINE}" > ${PKGDESTDIR}/etc/xbps.d/20-CRC-black-hole.conf
	}
}

CRC-cereus-core_package() {
	archs="x86_64* i686"
	short_desc="${short_desc} Cereus Linux Core repository"
	homepage="https://sourceforge.net/projects/cereus-linux/files/repos/cereus-core"
	pkg_install() {
		vmkdir etc/xbps.d
		echo "repository=${homepage}/${XBPS_TARGET_MACHINE}" > ${PKGDESTDIR}/etc/xbps.d/20-CRC-cereus-core.conf
	}
}

CRC-cereus-extra_package() {
	archs="x86_64* i686"
	short_desc="${short_desc} Cereus Linux Extra repository"
	homepage="https://sourceforge.net/projects/cereus-linux/files/repos/cereus-extra"
	pkg_install() {
		vmkdir etc/xbps.d
		echo "repository=${homepage}/${XBPS_TARGET_MACHINE}" > ${PKGDESTDIR}/etc/xbps.d/20-CRC-cereus-extra.conf
	}
}

CRC-d77void_package() {
	archs="x86_64"
	short_desc="${short_desc} d77 Void respin"
	homepage="https://sourceforge.net/projects/d77void/files/d77void-repo"
	pkg_install() {
		vmkdir etc/xbps.d
		echo "repository=${homepage}" > ${PKGDESTDIR}/etc/xbps.d/20-CRC-d77void.conf
	}
}

CRC-noid_package() {
	archs="x86_64"
	short_desc="${short_desc} Noid Linux repository"
	homepage="https://github.com/noid-linux/xbps-repo"
	pkg_install() {
		vmkdir etc/xbps.d
		echo "repository=${homepage}/releases/latest/download" > ${PKGDESTDIR}/etc/xbps.d/20-CRC-noid.conf
	}
}

CRC-odyssey_package() {
	archs="x86_64"
	short_desc="${short_desc} Odyssey Linux repository"
	homepage="https://code.odysseylinux.org/nobody"
	pkg_install() {
		vmkdir etc/xbps.d
		echo "repository=https://repo.odysseylinux.org/odyssey-repo/x86_64" > ${PKGDESTDIR}/etc/xbps.d/20-CRC-odyssey.conf
	}
}

CRC-sofijacom_package() {
	archs="x86_64"
	short_desc="${short_desc} Sofijacom repository"
	homepage="https://github.com/sofijacom/void-package"
	pkg_install() {
		vmkdir etc/xbps.d
		echo "repository=${homepage}/releases/latest/download" > ${PKGDESTDIR}/etc/xbps.d/20-CRC-sofijacom.conf
	}
}

CRC-sonicde_package() {
	archs="aarch64* x86_64* i686"
	short_desc="${short_desc} SonicDE repository by nerdyslacker"
	homepage="https://github.com/sonicde-void/sonicde-void"
	pkg_install() {
		vmkdir etc/xbps.d
		echo "repository=${homepage}/releases/latest/download" > ${PKGDESTDIR}/etc/xbps.d/20-CRC-sonicde.conf
	}
}

CRC-split_package() {
	archs="aarch64* x86_64*"
	short_desc="${short_desc} Split Linux repository"
	homepage="https://gitlab.com/splitlinux/split-packages"
	pkg_install() {
		vmkdir etc/xbps.d
		echo "repository=${homepage}/-/raw/master/binpkgs" > ${PKGDESTDIR}/etc/xbps.d/20-CRC-split.conf
	}
}

CRC-vostock_package() {
	archs="x86_64"
	short_desc="${short_desc} Vostock Linux repository"
	homepage="https://repo.vostoklinux.org/current"
	pkg_install() {
		vmkdir etc/xbps.d
		echo "repository=${homepage}" > ${PKGDESTDIR}/etc/xbps.d/20-CRC-vostock.conf
	}
}
